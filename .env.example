# Copy this file to ".env" and fill in your token.
# The real .env is gitignored and must NEVER be committed (this repo is public).
#
# Token needs: fine-grained PAT, repository = BetEseVendor, permission Contents: Read and write.
GITHUB_TOKEN=github_pat_xxxxxxxxxxxxxxxxxxxxxxxx
